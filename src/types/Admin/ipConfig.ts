export interface IpConfig {
  id: string;
  pms_site_id: string;
  protocol: string;
  camera_name: string;
  user_name: string;
  password: string;
  ip_address: string;
  port: string;
  site: string;
}